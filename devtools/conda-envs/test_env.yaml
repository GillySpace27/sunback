name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  -
  - pillow
  - astropy
  - ctypes
  - time
  - pathlib
  - numpy
  - urllib
  - pytesseract

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

