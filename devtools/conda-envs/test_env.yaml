name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - astropy
  - pathlib
  - numpy
  - sunpy[net]
  - pillow
  - appscript;platform_system=='Darwin'

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - ctypes;platform_system=='Windows'
      - urllib
      - time
#      - mpi4py

#    - codecov

