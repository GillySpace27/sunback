name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pillow
  - astropy
  - pathlib
  - numpy
  - pytesseract
  - tqdm
  - appscript

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - ctypes
      - urllib
      - time
#      - mpi4py

#    - codecov

