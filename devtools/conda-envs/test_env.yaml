name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - pillow
  - astropy
  - time
  - pathlib
  - numpy
  - pytesseract

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
      - ctypes
      - urllib
#      - mpi4py

#    - codecov

